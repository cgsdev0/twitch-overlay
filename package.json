{
  "name": "twitch-overlay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen-types": "node transform_schema.js && quicktype -o types.ts --lang typescript --src-lang schema --no-runtime-typecheck --just-types --prefer-unions --acronym-style camel './schema2.json#/definitions/' && ./gen-typemap"
  },
  "devDependencies": {
    "quicktype": "^23.0.11",
    "typescript": "^4.9.3",
    "vite": "^4.1.0",
    "vite-tsconfig-paths": "^4.0.5"
  }
}

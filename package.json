{
  "name": "twitch-overlay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen-types": "node types/transform_schema.js && quicktype -o src/tau_types.ts --lang typescript --src-lang schema --no-runtime-typecheck --just-types --prefer-unions --acronym-style camel './types/schema_fixed.json#/definitions/' && types/gen-typemap"
  },
  "devDependencies": {
    "quicktype": "^23.0.11",
    "typescript": "^4.9.3",
    "vite": "^4.1.0",
    "vite-tsconfig-paths": "^4.0.5"
  }
}
